# 实验 5-3 环境变量

# OpenAI API Key（必填）
OPENAI_API_KEY=sk-your-key-here

# 用作"小模型"代表的模型名（可选，默认 gpt-4o-mini；也可用 --small-model 覆盖）
MODEL=gpt-4o-mini

# 用作"大模型基线"的模型名（可选；设置后 demo.py 自动加跑第三臂，也可用 --big-model 覆盖）
# BIG_MODEL=gpt-4o
